import React from 'react';

const Membership = () => {
  return (
    <div style={{ height: '1000px' }}>
      TODO: (Not Logged In) Route to "/login" TODO: (Logged In) Show User Information (name, email,
      major, expected year of graduation) and a logout button
    </div>
  );
};

export default Membership;
