import React from 'react';

const Login = () => {
  return (
    <div style={{ height: '1000px' }}>
      TODO: (Not Logged In) show login form via Google Auth then ask for major and expected year of
      graduation TODO: (Logged In) Route to "/membership"
    </div>
  );
};

export default Login;
